// A very simple test to verify the testing environment works
describe('Basic Math', () => {
  test('1 + 1 equals 2', () => {
    expect(1 + 1).toBe(2);
  });

  test('2 - 1 equals 1', () => {
    expect(2 - 1).toBe(1);
  });

  test('2 * 2 equals 4', () => {
    expect(2 * 2).toBe(4);
  });
});
